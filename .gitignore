# ── Secrets (NEVER commit) ─────────────────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key
keystore/
*.keystore
# External marketplace participants' CAW credentials (Phase 6.5 self-service onboarding).
registry.local.json
**/registry.local.json
# Keyless discovery directory — runtime state (commit-safe, but not a repo artifact). Persists on AGENT_DATA_DIR.
registry.directory.json
**/registry.directory.json
# TSS key shares mounted into the signer container (Phase 6.5.4 Option B). NEVER commit.
keys/
/keys
keyshare.env
**/keyshare.env

# ── Node / pnpm (web) ──────────────────────────────────────────────────
node_modules/
.pnpm-store/
.next/
out/
dist/
*.tsbuildinfo
.vercel/

# ── Python (agents) ────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ── Foundry (contracts) ────────────────────────────────────────────────
contracts/out/
contracts/cache/
contracts/broadcast/
contracts/lib/

# ── OS / editor ────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

keyshares.tgz

# ── Internal working docs (kept local, NOT shipped) ───
CLAUDE.md
docs/FACTS.md
docs/STATUS.md
docs/DEMO_SCRIPT.md
docs/DEMO_PLAYBOOK.md
docs/DEMO_CHEATCARD.md
docs/DEPLOY_AGENTS.md
# Virtuals launch/partnership strategy — internal only (also lives in the private virtuals-adapter repo)
docs/LAUNCH_VIRTUALS.md
docs/PARTNERSHIP_VIRTUALS.md
agents/scripts/live_v4_keys.json
agents/scripts/live_v4_dispute_keys.json

# Local agent run/debug logs (never commit — noisy, may leak addresses)
agents/*.log

# TSS signer key-share bundle (base64 keyshares) — NEVER commit; scp to the VPS only. See docs/DEPLOY_SIGNER.md
tss.env
agents/tss/tss.env
.vercel
